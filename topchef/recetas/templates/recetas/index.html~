{% load staticfiles %}
<html>
	<head>
		<title>Top Chef</title>
		<link rel="stylesheet" type="text/css" href="{% static "recetas.css" %}">
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
			<a href="{% url 'addreceta' %}">Añadir Receta</a>
		{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		{% if lista_recetas %}
			<table border="2">
				<tr>
					<td>Nombre</td>
					<td>Fecha</td>
				</tr>
				{% for receta in lista_recetas %}
					<tr>
						<td><a href="{% url 'detalle_receta' receta.id %}">{{ receta.nombre }}</a></td>
						<td>{{ receta.fecha|date:"D d M Y - H:i" }}</td>
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay recetas</p>			
		{% endif %}
	</body>
</html>
